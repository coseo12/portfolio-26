# 보안 정책

## 에이전트 권한 원칙

- **최소 권한**: 각 에이전트는 역할에 필요한 최소한의 도구만 사용한다.
- **읽기 전용 기본**: 외부 도구(Gemini 등)는 항상 읽기 전용 모드로 실행한다.
- **파괴적 명령 차단**: `git push --force`, `git reset --hard`, `rm -rf` 등은 명시적으로 차단한다.

## 시크릿 관리

- `.env`, `credentials`, `*.key`, `*.pem` 파일은 절대 커밋하지 않는다.
- `.gitignore`에 민감 파일 패턴을 반드시 포함한다.
- 에이전트가 시크릿을 외부 도구(Gemini 등)에 전달하지 않도록 cross-validate 스킬에서 필터링한다.
- Auditor 에이전트가 시크릿 검출 도구(gitleaks 등)를 실행하여 하드코딩된 시크릿을 감지한다.

## 보안 리뷰 체크리스트

### 필수 (블로커)
- [ ] 하드코딩된 API 키, 비밀번호, 토큰 없음
- [ ] SQL 인젝션 방지 (파라미터화된 쿼리 사용)
- [ ] XSS 방지 (사용자 입력 이스케이프)
- [ ] CSRF 방지 (토큰 검증)
- [ ] 경로 순회(Path Traversal) 방지
- [ ] 명령어 인젝션 방지 (셸 이스케이프)
- [ ] 인증/인가 검증
- [ ] 민감 데이터 로깅 금지

### 권장
- [ ] HTTPS 강제
- [ ] 입력값 길이/형식 검증
- [ ] 에러 메시지에 내부 정보 노출 금지
- [ ] 의존성 취약점 없음 (npm audit / pip audit)
- [ ] 최소 권한 원칙 적용

## 사고 대응

1. 시크릿이 커밋된 경우: 즉시 시크릿 교체 + `git filter-branch` 또는 BFG로 히스토리 정리
2. 취약점 발견 시: `priority:critical` + `type:bug` 이슈 생성 → 즉시 수정
3. 에이전트 이상 동작: 오케스트레이터 킬 스위치 → 감사 로그 확인
